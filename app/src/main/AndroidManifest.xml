<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Location permissions -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_MOCK_LOCATION"
        tools:ignore="ProtectedPermissions,MockLocation" />

    <!-- Foreground service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Sprint 5: Overlay -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Sprint 5: Automation custom permission -->
    <permission
        android:name="com.ghostpin.permission.AUTOMATION"
        android:protectionLevel="signature"
        android:label="GhostPin Automation"
        android:description="@string/app_name" />

    <application
        android:name=".GhostPinApp"
        android:allowBackup="false"
        android:icon="@android:drawable/ic_menu_mylocation"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.GhostPin">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.GhostPin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Simulation foreground service -->
        <service
            android:name=".service.SimulationService"
            android:exported="false"
            android:foregroundServiceType="location" />

        <!-- Sprint 5: Floating overlay service -->
        <service
            android:name=".service.FloatingBubbleService"
            android:exported="false" />

        <!-- Sprint 5: Quick Settings tile -->
        <service
            android:name=".service.GhostPinQsTile"
            android:icon="@android:drawable/ic_menu_mylocation"
            android:label="GhostPin"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <meta-data
                android:name="android.service.quicksettings.ACTIVE_TILE"
                android:value="true" />
        </service>

        <!-- Sprint 5: Home screen widget -->
        <receiver
            android:name=".widget.GhostPinWidget"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/ghost_pin_widget_info" />
        </receiver>


        <receiver
            android:name=".scheduling.ScheduleReceiver"
            android:exported="false" />

        <receiver
            android:name=".scheduling.BootCompletedReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Sprint 5: Automation broadcast receiver -->
        <receiver
            android:name=".automation.AutomationReceiver"
            android:exported="true"
            android:permission="com.ghostpin.permission.AUTOMATION">
            <intent-filter>
                <action android:name="com.ghostpin.ACTION_START" />
                <action android:name="com.ghostpin.ACTION_STOP" />
                <action android:name="com.ghostpin.ACTION_PAUSE" />
                <action android:name="com.ghostpin.ACTION_SET_ROUTE" />
                <action android:name="com.ghostpin.ACTION_SET_PROFILE" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
